import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  txtP: {
    fontSize: 18,
    color: 'black',
    textAlign: 'center',
  },
  
  txtG: {
    fontSize: 32,
    color: 'black',
    textAlign: 'center',
  },
});
